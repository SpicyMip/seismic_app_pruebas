import 'package:flutter/material.dart';

class Nosotros extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text(
      '[Info acerca AZP group y Seismic App] ',
      style: TextStyle(fontSize: 30.0),
    ));
  }
}
